# 🔐 Secrets Template
# Rename this file to secrets.yaml and fill in the values
# Install: helm install crystolia ./crystolia-chart -f secrets.yaml

secrets:
  greenInvoice:
    apiId: "YOUR_API_ID"
    secret: "YOUR_SECRET"
    sandbox: "false"
  whatsapp:
    accessToken: "YOUR_TOKEN"
    phoneNumberId: "YOUR_PHONE_ID"
    webhookVerifyToken: "YOUR_VERIFY_TOKEN"
  twilio:
    accountSid: "YOUR_SID"
    authToken: "YOUR_AUTH_TOKEN"
    phoneNumber: "YOUR_NUMBER"
  payment:
    provider: "meshulam"
