# Default values for crystolia-chart.

global:
  environment: "production"

frontend:
  image:
    repository: 268456953512.dkr.ecr.us-east-1.amazonaws.com/crystolia-frontend
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 3000
  replicaCount: 2

backend:
  image:
    repository: 268456953512.dkr.ecr.us-east-1.amazonaws.com/crystolia-backend
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 3000
  replicaCount: 2
  env:
    MONGO_URI: "mongodb://mongo:27017/crystolia"
    FRONTEND_URL: "https://crystolia.com"
    GOOGLE_CLIENT_ID: "42819184159-tqh018b0d0dkhv6sc37abov9rmjkgaer.apps.googleusercontent.com"
    GOOGLE_CLIENT_SECRET: "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"
    JWT_SECRET: "crystolia-super-secret-jwt-key-2026"

mongodb:
  image:
    repository: mongo
    tag: 6.0
  service:
    port: 27017
  persistence:
    enabled: false
    size: 5Gi
  backup:
    enabled: true
    schedule: "0 2 * * *"  # Daily at 2 AM
    s3Bucket: ""  # crystolia-backups
    awsRegion: "us-east-1"

ingress:
  enabled: true
  host: crystolia.com
  hostWWW: www.crystolia.com
  # hostIL: crystolia.co.il  # Disabled until DNS is configured
  tls:
    enabled: true

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔐 Production Secrets (Override via --set or secrets.yaml)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
secrets:
  greenInvoice:
    apiId: ""
    secret: ""
    sandbox: "false"
  whatsapp:
    accessToken: ""
    phoneNumberId: ""
    webhookVerifyToken: ""
  twilio:
    accountSid: ""
    authToken: ""
    phoneNumber: ""
  payment:
    provider: "mock"  # mock or meshulam

