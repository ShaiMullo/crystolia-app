# ⚠️ LEGACY WORKFLOW - REPLACED BY production.yml and staging.yml
name: Legacy Deploy

on:
  push:
    branches: [ "legacy-disabled" ]
    # Disabled triggering on main to prevent conflicts

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use Staging/Production Deployment workflows."
