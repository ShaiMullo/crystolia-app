# ===============================================
# 🚀 Crystolia CI/CD Pipeline
# ===============================================
# קובץ זה רץ אוטומטית בכל push או PR ל-main
# הוא בודק את הקוד, בונה את האפליקציה, ודוחף Docker image

name: CI/CD Pipeline

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📅 מתי ה-Pipeline ירוץ?
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
on:
  push:
    branches: [ main ] # בכל push ל-branch main
  pull_request:
    branches: [ main ] # בכל Pull Request ל-main

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔧 משתני סביבה גלובליים
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
env:
  DOCKER_USERNAME: shaimullo
  IMAGE_NAME: crystolia-frontend

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📋 ה-Jobs (המשימות שירוצו)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
jobs:

  # ═══════════════════════════════════════════
  # Job 1: Build & Test
  # ═══════════════════════════════════════════
  build:
    name: 🔨 Build & Test
    runs-on: ubuntu-latest # מכונה וירטואלית עם Ubuntu

    steps:
      # ───────────────────────────────────────
      # Step 1: שליפת הקוד מ-GitHub
      # ───────────────────────────────────────
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        # זה שולף את כל הקוד מה-repository למכונה

        # ───────────────────────────────────────
        # Step 2: התקנת Node.js
        # ───────────────────────────────────────
      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # גרסת Node
          cache: 'npm' # שמירת cache להאצת builds עתידיים
          cache-dependency-path: frontend/package-lock.json

      # ───────────────────────────────────────
      # Step 3: התקנת dependencies
      # ───────────────────────────────────────
      - name: 📚 Install dependencies
        working-directory: frontend
        run: npm ci
        # npm ci = התקנה נקייה לפי package-lock.json (מהיר יותר מ-npm install)

        # ───────────────────────────────────────
        # Step 4: בדיקת Lint (סגנון קוד)
        # ───────────────────────────────────────
      - name: 🔍 Run ESLint
        working-directory: frontend
        run: npm run lint
        # בודק שהקוד עומד בסטנדרטים של הפרויקט

        # ───────────────────────────────────────
        # Step 5: Build הפרויקט
        # ───────────────────────────────────────
      - name: 🏗️ Build application
        working-directory: frontend
        run: npm run build
        # בונה גרסת production של האפליקציה

        # ═══════════════════════════════════════════
        # Job 2: Build & Push Docker Image
        # ═══════════════════════════════════════════
  docker:
    name: 🐳 Docker Build & Push
    runs-on: ubuntu-latest
    needs: build # ⚠️ ירוץ רק אחרי ש-build הצליח!

    # רק ב-push ל-main (לא ב-PR)
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      # ───────────────────────────────────────
      # Step 1: שליפת הקוד
      # ───────────────────────────────────────
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      # ───────────────────────────────────────
      # Step 2: הגדרת Docker Buildx
      # ───────────────────────────────────────
      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        # Buildx = כלי בנייה מתקדם של Docker (תומך ב-caching וכו')

        # ───────────────────────────────────────
        # Step 3: Login ל-Docker Hub
        # ───────────────────────────────────────
      - name: 🔐 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
        # ⚠️ הסיסמה נשמרת ב-GitHub Secrets (נגדיר אחר כך)

        # ───────────────────────────────────────
        # Step 4: יצירת תגיות (tags) ל-image
        # ───────────────────────────────────────
      - name: 🏷️ Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest
        # יוצר 2 תגיות:
        #   1. SHA של הקומיט (לזיהוי מדויק)
        #   2. "latest" (הגרסה האחרונה)

        # ───────────────────────────────────────
        # Step 5: Build & Push Docker Image
        # ───────────────────────────────────────
      - name: 🚀 Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha # שימוש ב-cache של GitHub Actions
          cache-to: type=gha,mode=max
        # בונה את ה-Docker image ודוחף ל-Docker Hub
