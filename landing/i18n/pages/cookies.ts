// ⚠️ DRAFT SCAFFOLDING — intentionally EMPTY.
// No /cookies route renders this yet. Real, counsel-reviewed Cookie Policy
// content (EN / HE / RU) lands in Compliance PR-4. Do NOT treat anything here as
// a published policy. Keep this empty until the reviewed copy is added.
import type { LegalContent } from "./legalTypes";

// TODO(PR-4): populate with the reviewed Cookie Policy per locale
// (including the cl_loc cookie inventory + consent categories).
export const cookiesContent: LegalContent = {};
