# 🐳 Crystolia - Docker Compose
# ===============================================
# Run: docker compose up
# Run in background: docker compose up -d
# Stop: docker compose down

services:
  # ─────────────────────────────────────────────
  # 🍃 MongoDB - Database
  # ─────────────────────────────────────────────
  mongo:
    image: mongo:6.0
    ports:
      - "27017:27017"
    volumes:
      - db_data:/data/db
    restart: unless-stopped
  # ─────────────────────────────────────────────
  # 🎨 Frontend - Next.js
  # ─────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000" # localhost:3000 → container:3000
    environment:
      - NODE_ENV=production
      - BACKEND_URL=http://backend:4000/api
      - NEXT_PUBLIC_API_URL=http://localhost:4000/api
    depends_on:
      - backend # מחכה ש-backend יעלה קודם
    restart: unless-stopped # אוטומטית קם מחדש אם נופל

  # ─────────────────────────────────────────────
  # 🔧 Backend - Express.js
  # ─────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "4000:4000" # localhost:4000 → container:4000
    environment:
      - NODE_ENV=production
      - PORT=4000
      - FRONTEND_URL=http://localhost:3000
      # WhatsApp Cloud API
      - WHATSAPP_ACCESS_TOKEN=${WHATSAPP_ACCESS_TOKEN}
      - WHATSAPP_PHONE_NUMBER_ID=${WHATSAPP_PHONE_NUMBER_ID}
      - WHATSAPP_WEBHOOK_VERIFY_TOKEN=${WHATSAPP_WEBHOOK_VERIFY_TOKEN}
      - MONGO_URI=mongodb://mongo:27017/crystolia
      # Social Auth Keys (Replace with real values)
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-483698534098-qvlmk53c5v53lel1bj7c67fa921cskm0.apps.googleusercontent.com}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-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}
      # - APPLE_SERVICE_ID=...
      # - APPLE_TEAM_ID=...
      # - APPLE_KEY_ID=...
      # - APPLE_PRIVATE_KEY_PATH=...
    depends_on:
      - mongo
    restart: unless-stopped

# ─────────────────────────────────────────────
# 💾 Volumes (לעתיד - מסד נתונים)
# ─────────────────────────────────────────────
volumes:
  db_data:
